# Manual: Enable auto-merge for the repository

If enabling auto-merge fails:

1. In GitHub, open the repository.
2. Go to **Settings > General** (or **Settings > Options** depending on UI).
3. Under the pull request section, ensure **Allow auto-merge** is enabled.
4. Save the settings, then rerun the utility.
